---
description: Automatically push changes to the remote git repository
---

1. Stage all changes
   // turbo
   git add .

2. Commit changes with a generic or provided message
   // turbo
   git commit -m "Auto-update: applied changes"

3. Push to the main branch
   // turbo
   git push origin main
